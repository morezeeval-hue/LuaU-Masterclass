document.querySelectorAll('.quiz button').forEach(button=>{button.addEventListener('click',()=>{const r=document.getElementById('result');if(button.dataset.answer==='right'){r.textContent='Richtig. Der Server bleibt die Quelle der Wahrheit.';r.className='ok'}else{r.textContent='Falsch. Ein Exploiter kann Client-Code manipulieren.';r.className='bad'}})});document.querySelectorAll('pre code').forEach(code=>{const b=document.createElement('button');b.textContent='Code kopieren';b.className='copy';b.addEventListener('click',async()=>{await navigator.clipboard.writeText(code.textContent);b.textContent='Kopiert';setTimeout(()=>b.textContent='Code kopieren',1200)});code.parentElement.before(b)});